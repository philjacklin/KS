import { describe, it, expect } from 'vitest';
import KiwiSaver from './KiwiSaver.svelte';

describe('KiwiSaver component', () => {
    it('should exist', () => {
        expect(KiwiSaver).toBeDefined();
    });
});
